{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Clearance list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Issuedat</th>
                <th>Requestedat</th>
                <th>Notes</th>
                <th>Requestkey</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('clearance_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.issuedAt %}{{ entity.issuedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.requestedAt %}{{ entity.requestedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.notes }}</td>
                <td>{{ entity.requestKey }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('clearance_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('clearance_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('clearance_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
